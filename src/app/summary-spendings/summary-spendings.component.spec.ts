/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SummarySpendingsComponent } from './summary-spendings.component';

describe('Component: SummarySpendings', () => {
  it('should create an instance', () => {
    let component = new SummarySpendingsComponent();
    expect(component).toBeTruthy();
  });
});
