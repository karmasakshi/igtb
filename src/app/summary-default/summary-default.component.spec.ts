/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SummaryDefaultComponent } from './summary-default.component';

describe('Component: SummaryDefault', () => {
  it('should create an instance', () => {
    let component = new SummaryDefaultComponent();
    expect(component).toBeTruthy();
  });
});
